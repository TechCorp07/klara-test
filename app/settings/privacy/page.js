import PrivacyClient from './components/PrivacyClient';

/**
 * Server component for privacy settings page
 * This simply renders the client component that handles the privacy settings functionality
 */
export default function Page() {
  return <PrivacyClient />;
}