import LanguageClient from './components/LanguageClient';

/**
 * Server component for language settings page
 * This simply renders the client component that handles the language settings functionality
 */
export default function Page() {
  return <LanguageClient />;
}