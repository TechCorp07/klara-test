import LoginClient from './components/LoginClient';

/**
 * Server component for login page
 * This simply renders the client component that handles the login functionality
 */
export default function Page() {
  return <LoginClient />;
}