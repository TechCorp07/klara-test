import LoginClient from "./components/LoginClient"

/**
 * Login page component that renders the login client
 * This is the main entry point for the /auth/login route
 */
export default function LoginPage() {
  return <LoginClient />
}
