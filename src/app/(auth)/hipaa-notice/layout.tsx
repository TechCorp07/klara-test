// src/app/(auth)/hipaa-notice/layout.tsx
import React from 'react';

interface HipaaNoticeLayoutProps {
  children: React.ReactNode;
}

export default function HipaaNoticeLayout({ children }: HipaaNoticeLayoutProps) {
  return children;
}